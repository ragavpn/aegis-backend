# nixpacks.toml — Railway build configuration for aegis-backend
# Using custom Dockerfile for multi-language (Node + Python/Kokoro) support.
# Railway will auto-detect the Dockerfile and use it instead of nixpacks.
